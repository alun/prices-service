[project]
name = "prices-service"
version = "0.1.0"
description = "HTTP service for TradingView OHLCV price data with SQLite caching"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "websocket-client>=1.6.0",
    "python-dotenv>=1.2.1",
]
